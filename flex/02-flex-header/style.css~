.header  {
  font-family: monospace;
  background: papayawhip;
  display: flex;
  justify-content: space-between;
  align-items: center;
  height: 75px;
}

.logo {
  font-size: 48px;
  font-weight: 900;
  color: tomato;
  background: white;
  padding: 4px 32px;
}

* {
  border: 2px solid red;
}

ul {
  /* another display: flex is needed? possibly only to offset default <ul> values? */
  display: flex;
  flex-direction: row;
  /* missing a mrgin: and padding: properties in initial attempt */
  margin: 0;
  padding: 0;
  column-gap: 10px;
  /* this removes the dots on the list items*/
  list-style-type: none;
}

a {
  font-size: 22px;
  background: white;
  padding: 8px;
  /* this removes the line under the links */
  text-decoration: none;
}
